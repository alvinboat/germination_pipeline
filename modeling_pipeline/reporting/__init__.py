"""Figures and summaries for a training session.

`report_run.py` is the entry point. Training scripts emit a run JSON plus a
predictions .npz; everything here reads those, so a report can be regenerated or
restyled without retraining anything.
"""
